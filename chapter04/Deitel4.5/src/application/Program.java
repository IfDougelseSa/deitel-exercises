package application;

public class Program {

	public static void main(String[] args) {

		/**
		 * 
		 * Escreva uma instrução Java para realizar cada uma das seguintes tarefas: a)
		 * Declarar variáveis sum do tipo int e inicialize-as como 0. b) Declarar
		 * variáveis x do tipo int e inicialize-as como 1. c) Adicione a variável x à
		 * variável sum, e atribua o resultado à variável sum. d) Imprima "A soma é: "
		 * seguido pelo valor da variável sum.
		 * 
		 * 
		 * Write a Java statment to perform which of the following tasks: a) Declare sum
		 * variables of int type and initialize them to 0. b) Declare x variables of int
		 * type and initialize them to 0. c) Add x variable to sum variable, and assign
		 * the result to sum variable. d) print "The sum is: " followed by the value of
		 * the sum variable.
		 */

		int sum = 0;
		int x = 1;

		sum += x;
		System.out.println("A some é: " + sum);
	}

}
